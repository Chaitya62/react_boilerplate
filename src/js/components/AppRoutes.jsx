import React, {Component} from 'react';
import {Route} from 'react-router-dom';
const AppRoutes = (props) => {
  return (
    <div>
      <Route exact path='/' component={Error : no source code supplied to jspretty !}/>
      <Route path='/' component={Error : no source code supplied to jspretty !}/>
    </div>
  );
}

export default AppRoutes;
